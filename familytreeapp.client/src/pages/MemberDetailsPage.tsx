import { Container, Typography } from '@mui/material';

export default function MemberDetailsPage() {
  return (
    <Container maxWidth="lg" sx={{ mt: 4, mb: 4 }}>
      <Typography variant="h4">Member Details Page</Typography>
    </Container>
  );
}